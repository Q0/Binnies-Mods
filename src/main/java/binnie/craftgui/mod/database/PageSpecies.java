package binnie.craftgui.mod.database;

import binnie.craftgui.core.IWidget;

public abstract class PageSpecies extends PageAbstract {
    public PageSpecies(IWidget parent, DatabaseTab tab) {
        super(parent, tab);
    }
}
